---
'create-hana': minor
---

Introduce a managed template dependency registry with preset-based resolution, migrate generators away from hard-coded package versions, remove invalid `hono/*` pseudo dependencies from generated `package.json`, and add template smoke-test CI plus Renovate support for registry-managed updates.
